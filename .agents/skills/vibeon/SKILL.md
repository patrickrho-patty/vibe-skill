---
name: vibeon
description: Enable auto-delegate mode — all coding tasks are automatically sent to a delegate harness without requiring $vibe each time.
---

# $vibeon

Run: `mkdir -p .delegate && touch .delegate/auto.flag`

Then confirm: "Auto-vibe ON — every coding request will be delegated automatically."

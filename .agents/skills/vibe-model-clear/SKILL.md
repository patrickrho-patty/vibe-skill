---
name: vibe-model-clear
description: Clear the model override and revert to the config default.
---

# $vibe-model-clear

Run: `rm -f .delegate/model.flag`

Confirm: "Model override cleared — delegates will use the config default."
